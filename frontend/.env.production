# Production environment variables
# API_GATEWAY_URL and NEXT_PUBLIC_API_BASE_URL are set via Amplify console env vars.
# Do NOT set them here — empty string values override Amplify-injected env vars
# because dotenv sets "" before the Lambda runtime env is applied.
