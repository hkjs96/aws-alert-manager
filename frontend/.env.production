# Production — set API_GATEWAY_URL to the deployed API Gateway endpoint.
# All /api/* requests will be proxied to this URL (stripping the /api prefix).
# Example: https://<api-id>.execute-api.ap-northeast-2.amazonaws.com/prod
API_GATEWAY_URL=

NEXT_PUBLIC_API_BASE_URL=
