# 실제 API Gateway 엔드포인트 (CloudFormation Outputs > ApiEndpointUrl)
# 설정 시 Next.js Mock API 대신 실제 백엔드를 사용합니다
# 미설정 시 로컬 Mock API로 동작합니다
NEXT_PUBLIC_API_BASE_URL=https://REPLACE_WITH_API_GATEWAY_URL/prod
